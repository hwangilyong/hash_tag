import * as types from '../../action/ActionTypes';

const initialState = {

}

export default function auth(state = initialState, action) {
    switch (action.type) {
        default:
            return {...state}
    }
}